export declare class User {
    constructor(name: string, username: string, password: string, email: string, profilePicture: string, insertDate: Date, id?: number);
    id: number;
    name: string;
    username: string;
    password: string;
    email: string;
    profilePicture: string;
    insertDate: Date;
}
